#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
    int n,x;
    cin>>n>>x;
    vector<int> coins(n);
    vector<ll> times(n+1,0);
    for(int i=0;i<n;i++){
        cin>>coins[i];
    }
    
    return 0;
}
